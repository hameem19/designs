Adapter Design Pattern is a Creational Design Pattern 

Recognizeable by creational methods which returns an implementation of given abstract/interface 
type which in turn delegates/uses a different implementation of given abstract/interface type

1). java.lang.reflect.Proxy
2). java.rmi.*, the whole API actually.